let array1 = []; // Tạo mảng rỗng
let array2 = new Array(); // Tạo mảng rỗng
let fruits = ["Táo", "Chuối", "Cam"]; // Tạo mảng với các phần tử

console.log(fruits[0]); // Táo
console.log(fruits[1]); // Chuối
console.log(fruits[2]); // Cam

console.log(fruits.length); // 3

console.log(Array.isArray(fruits)); // true
